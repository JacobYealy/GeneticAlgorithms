import pandas as pd


def preprocess_data(df):
    """
        Preprocessing logic
        """
pass


def preprocess_simulated_data(simulated_data):
    """
    Preprocess the simulated data.

    Parameters:
    simulated_data (DataFrame): The simulated data to be preprocessed.

    Returns:
    DataFrame: The preprocessed simulated data.
    """
    return preprocess_data(simulated_data)


def preprocess_real_world_data(real_world_data):
    """
    Preprocess the real-world data.

    Parameters:
    real_world_data (DataFrame): The real-world data to be preprocessed.

    Returns:
    DataFrame: The preprocessed real-world data.
    """
    return preprocess_data(real_world_data)


